#' List Available Palettes in the okcolors Package
#'
#' Returns a character vector of all valid palette names supported by the `okcolors` package.
#'
#' @return A character vector with the names of available palettes.
#' @examples
#' valid_palettes()
#' @export
valid_palettes <- function() {
  c("it", "stone", "skyscrapers", "obsession", "moment")
}
